# Base URL for the application (useful for deploying to a subdirectory)
# Example: /pdf-splitter/ if deploying to https://example.com/pdf-splitter/
VITE_BASE_URL=/

# Set to 'true' to generate a zip archive of the build output (dist.zip)
VITE_BUILD_ZIP=false
